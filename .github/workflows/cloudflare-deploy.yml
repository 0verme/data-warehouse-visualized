name: Cloudflare deploy

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Test, build, and deploy
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.event_name == 'pull_request' && format('cloudflare-validation-pr-{0}', github.event.pull_request.number) || 'cloudflare-production' }}
      cancel-in-progress: true
    steps:
      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22.x
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm run test

      - name: Run lint
        run: npm run lint

      - name: Check formatting
        run: npm run format:check

      - name: Run type check
        run: npm run check

      - name: Build static site
        run: npm run build

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Run browser smoke
        run: npm run test:e2e:smoke -- --skip-build

      - name: Deploy to Cloudflare Workers
        if: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' }}
        uses: 0verme/ci-workflows/.github/actions/cloudflare-worker-deploy@v1
        with:
          working_directory: .
          api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          account_id: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
